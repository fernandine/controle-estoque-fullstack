import { StatusRolePipe } from './status-role.pipe';

describe('StatusRolePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusRolePipe();
    expect(pipe).toBeTruthy();
  });
});
